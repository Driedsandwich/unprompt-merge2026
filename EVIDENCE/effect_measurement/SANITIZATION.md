# 公開前の伏字化記録 (SANITIZATION)

本ディレクトリの測定証跡は「生成されたバイト列をそのまま保存・公開する」方針だが、公開前スキャンで個人情報の混入が1件見つかったため、以下の伏字化を行った。**これが判定 (ブラインド二択) 完了後に加えた唯一の改変である。**

## 実施内容 (2026-08-08)

- 対象: `pairs_v3/m05-portfolio/compiled.html` および `blind_v3/m05-portfolio/left.html` (同一生成物の判定用コピー)
- 変更: 連絡先欄の実在メールアドレス (計4箇所: mailto属性2+表示文字列2) を `contact@example.com` へ置換
- それ以外のバイトは無変更。判定は伏字化前のファイルで実施済みであり、判定結果への影響はない (メールアドレス自体は判定基準のどの項目にも該当しない)

## 経緯と特記事項

- 当該メールアドレスは、この測定の入力側 (ブリーフ `sessions/m05-portfolio/brief.txt`・発注者ペルソナメモ `owner_memos.md`・指示書 `handoff_v2.json`) のいずれにも存在しない (grep実測0件)。同条件の raw 側生成物にも存在しない
- すなわち生成モデル (`claude -p`・サブスクリプション認証) が、入力に含まれない実在のメールアドレスを何らかの経路で成果物に補完したものとみられる (経路は未特定)
- この観測自体が「AI生成物の公開前には、入力の清浄性によらず生成物そのものへのPIIスキャンが必要」という教訓であり、記録として残す
