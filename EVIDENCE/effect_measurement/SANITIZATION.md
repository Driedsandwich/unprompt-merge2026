# 公開前の伏字化記録 (SANITIZATION)

本ディレクトリの測定証跡は「生成されたバイト列をそのまま保存・公開する」方針だが、公開前スキャンで個人情報の混入が1件見つかったため、以下の伏字化を行った。**これが判定 (ブラインド二択) 完了後に加えた唯一の改変である。**

## 実施内容 (2026-08-08)

- 対象: `pairs_v3/m05-portfolio/compiled.html` および `blind_v3/m05-portfolio/left.html` (同一生成物の判定用コピー)
- 変更: 連絡先欄の実在メールアドレス (計4箇所: mailto属性2+表示文字列2) を `contact@example.com` へ置換
- それ以外のバイトは無変更。判定は伏字化前のファイルで実施済みであり、判定結果への影響はない (メールアドレス自体は判定基準のどの項目にも該当しない)

## 経緯と特記事項

- 当該メールアドレスは、この測定の入力側 (ブリーフ `sessions/m05-portfolio/brief.txt`・発注者ペルソナメモ `owner_memos.md`・指示書 `handoff_v2.json`) のいずれにも存在しない (grep実測0件)。同条件の raw 側生成物にも存在しない
- すなわち生成モデル (`claude -p`・サブスクリプション認証) が、入力に含まれない実在のメールアドレスを何らかの経路で成果物に補完したものとみられる (経路は未特定)
- この観測自体が「AI生成物の公開前には、入力の清浄性によらず生成物そのものへのPIIスキャンが必要」という教訓であり、記録として残す

## 追記 (2026-08-10・v4)

v4 の新規生成物 `pairs_v4/m05-portfolio/compiled.html` (と blind_v4/ の同一物 art_2.html) に、v1 と同じ実メールアドレスがモデル起源で再出現した (各2箇所・計4箇所)。入力側 (brief / handoff_v4.json / 判定プロンプト) には当該文字列は存在しない (grep 0件で確認)。判定完了後に `contact@example.com` へ置換した。判定3体の判定文には当該文字列は含まれない (grep 0件)。この置換は判定後に加えた唯一の改変である。同一モデル (sonnet) がポートフォリオ文脈で同一の実在アドレスを繰り返し出力する事象は、製品側の出力ゲート (公開前スキャン) の必要性を裏付ける再現実例として記録する。

## 追記 (2026-08-19・v5)

v5 の新規生成物 `pairs_v5/m05-portfolio/compiled.html` (と blind_v5/ の同一物 art_2.html) に、v1/v4 と同じ実メールアドレスがモデル起源で**3度目の再出現**をした (各2箇所)。入力側 (brief / handoff_v4.json / owner_memos.md) に当該文字列は存在しない (grep 0件で確認)。v5 では生成直後の定期スキャンで**判定前に発見**したため、判定開始前に `〈メールアドレス〉` へ置換した (v4 の事後置換 `contact@example.com` と置換文字列が異なるのは、v5 の COMPILED_EXTRA 二律がプレースホルダ〈〉を正規の作法として定めており、判定者が見る成果物として最も中立なため)。判定者は置換後のファイルのみを見る。注: 二律「具体的事実は創作せずプレースホルダにする」を追加した版でも、実在アドレスの混入自体は防げなかった (混入はプロンプト規律でなく出力ゲートで止めるべきという v1/v4 の教訓を再々確認)。

## 追記 (2026-08-19・v6)

v6 の新規生成物では、従来の m05 でなく **`pairs_v6/m03-scout/compiled.html` (と blind_v6/ の同一物 art_1.html) に同じ実メールアドレスが4度目の混入** (各1箇所・スカウトメールの返信先文脈)。入力側に当該文字列なし (grep 0件)。v5 と同じく判定前スキャンで検知し `〈メールアドレス〉` へ置換、判定者は置換後のみを見る。出現箇所が成果物型 (連絡先を書く型) に追随して移ることから、モデルが「連絡先の実値」を要求される文脈で特定の実在アドレスを補完する傾向が示唆される。
