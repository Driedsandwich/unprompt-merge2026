# 指示書効果測定 — 事前登録 (Brief Effect Measurement)

状態: **ドラフト (ブリーフ10本の確定待ち)**。確定時にこのファイルをローカルコミットして固定し、以後は追記のみ (結果を見てからの設計変更・ブリーフ差し替えを構造的に不可能にする)。Wave 1 審査期間中は push しない (提出物凍結)。公開は Wave 2 開始 (2026-08-10 23:59 JST) 以降。

## 命題

同一の曖昧な一文について、下流のAI制作者に

- **A**: 一文だけを渡す
- **B**: 同じ一文を Unprompt に通して発注者本人が〈決める/委ねる〉した**手渡しJSON** (`unprompt.compiled_brief.v0`) を渡す

とき、B の成果物の方が**発注者本人の意図に近い**。

## 手続き

1. **ブリーフ確定**: 下記候補12本から発注者 (=ユーザー本人) が10本を選定・自由に編集 (自作差し替え可)。確定した10本をこのファイルに焼き付けてローカルコミット (事前登録タイムスタンプ)。
2. **選択セッション**: 発注者が実アプリ (QA常駐 127.0.0.1:8512) で1本ずつ通常フローを実行 (爆散→本人の意図で決める/委ねる→指示書)。「指示書をコピーしてAIに渡す」で得たJSONを `sessions/<id>/handoff.json` に保存。一文は `sessions/<id>/brief.txt`。
   - 選択は本人の実意図で行う (演出のための対照的選択はしない — 既存デモペアとの違い)。
   - 委ねる判断も本人の実感覚で可 (委任も製品の正規の出力)。
3. **生成**: `effect_measure.py` が各ブリーフについて A/B を生成。公平条件は既存並置ペアの拘束を継承: 同一モデル (sonnet)・同一 effort (low)・同一の制作者 system 骨格+デザイン憲章 (`prompts/downstream_maker_v1.txt`)。違いは user prompt (Aは一文 / BはJSON) と、B側のみのコンパイル済みブリーフ取扱2項 (既存ペアと同一の非対称・manifest に全文開示)。A/B とも同一試行回数規則 (HTML不成立時のみ最大2回、記録を残す)。
4. **ブラインド判定 (主指標)**: `eval_blind.html` が10ペアを**ブリーフごとに決定論シャッフルした左右順**で提示 (条件ラベル非表示)。発注者は各ペアについて「自分の意図に近いのはどちらか」(強制二択)+確信度 (1-5) を回答。回答完了までA/Bの対応は開示しない。
5. **副指標**: 各ペアについて、handoff.json の決定事項が成果物に反映されているかのチェックリスト照合 (決定ごとに 反映/部分/なし。機械補助+人裁定・裁定理由を記録)。
6. **第三者判定 (統制)**: 独立エージェント3体が同じブラインド提示で判定 (発注者バイアスの統制)。

## 報告規律

- **n=10・発注者1名の実測**であり、統計的一般化は主張しない。効果の言明は「この10本・この発注者での実測」に限定する。
- **全ペア・全判定・負けたペアを含む生データを公開** (EVIDENCE/effect_measurement/)。選り好みをしない。
- 事前登録後の手続き変更は、変更理由ごと追記で開示する。

## 時間等価比較 (併設・Milestone宣言分)

同一ブリーフのうち3本について、逐次テキスト質問 (チャットで1問ずつ確定) で同等の確定に至るまでの所要時間とタイプ文字数を実測し、Unprompt セッションの実測 (タイプ0字・所要秒) と併記する。

## ブリーフ候補12本 (10本を選定・編集して確定)

| id | 一文 (案) | 成果物 | 型 |
|---|---|---|---|
| m01-lp | かっこいいけど信頼感もあるLPを作って。うちのサービス用。 | LP | page |
| m02-event | 秋にやる社内勉強会の告知ページ、いい感じにして。 | 告知ページ | page |
| m03-scout | 若手エンジニアに刺さるスカウトメール書いて。 | スカウトメール | text |
| m04-ec | この新商品の説明文、魅力的にお願い。 | 商品説明文 | text |
| m05-portfolio | 転職用のポートフォリオサイト、シンプルだけど印象に残る感じで。 | ポートフォリオ | page |
| m06-news | 月イチの社内報の巻頭あいさつ、堅すぎない感じで。 | 挨拶文 | text |
| m07-recruit | 採用ページの会社紹介、飾らないけど熱意が伝わるように。 | 採用ページ | page |
| m08-apology | 発送遅延のお詫びメール、誠実だけど重すぎないトーンで。 | お詫びメール | text |
| m09-spec | 社内の経費精算をラクにする小さなツールの仕様メモを書いて。 | 仕様メモ | text |
| m10-invite | 昔の仲間を集める同窓会の案内、気軽に来たくなるやつ。 | 案内文 | text |
| m11-brand | 新しいコーヒー豆ブランドの世界観が伝わる紹介ページ。 | 紹介ページ | page |
| m12-faq | 問い合わせが多い返品まわりのFAQページ、探しやすく安心感のある感じ。 | FAQページ | page |

備考: 既存デモ5ペアの一文と重複しない新規文とし、ドメイン幅 (page/text・ビジネス/私的・制作/実務) を持たせた。maker/structure の spec は effect_measure.py 内に定義し、確定時に本ファイルへ転記する。

---

## 追補1 (2026-07-31・確定): AI発注者ペルソナ版として実施

ユーザー指示「Fable 5で代行して進める」により、発注者役をAIペルソナに変更して先行実施する。人間発注者による追試 (3本以上) は任意の後続として残す。

- **ブリーフ確定**: 候補の先頭10本 (m01〜m10) を機械的に採用 (選り好み排除)。m11/m12 は不使用。
- **意図の外在化**: 各ブリーフの発注者像・実現したいこと・好み・禁忌を `owner_memos.md` に**選択肢を見る前に固定** (Fable 5 独立エージェントが依頼文のみから生成。製品知識なし)。以後このメモが選択と判定の唯一の根拠。
- **選択セッション**: `build_session.py --explode` で判断点と選択肢を抽出→発注者役エージェント (Fable 5) がメモのみを根拠に各判断点を決定→`--picks` で render/compile を実行し手渡しJSON (`unprompt.compiled_brief.v0`、参照実装 `EVIDENCE/media_pairs/keyvisual_compiled_brief.json` と同形) を構築。**本版では全判断点を「決める」とし委任は使わない** (スキーマ簡素化のため。開示)。
- **ブラインド判定**: 発注者役とは別の新規エージェントが、メモ+成果物ペア (条件ラベルなし・決定論左右順) のみで判定。第三者判定はメモなし版も併走。
- **開示**: 本測定の発注者・判定者はAIであり、人間発注者の実測ではない。この限定は結果報告に必ず付す。

---

## 結果1 (2026-07-31・AI発注者版 v1 の実測 — 事前登録どおり全公開)

- 発注者判定 (主指標): **compiled 2/10・raw 8/10 — 仮説と逆**
- 第三者判定1 (一文のみ): compiled 8/10
- 第三者判定2 (修正往復視点): compiled 5/10
- 生データ: judgments/ (3判定の全文)・decoded_tally.json。復号バグは実ファイル検証で否定 (compiled側のみ選択マーカーとトークン実装を確認)。

### 原因分析 (実ファイルに基づく)

発注者役の選択は**ラベル文字列のみ**を見て行われた (本測定v1の手続きはサムネイル見本を提示していない)。ラベル水準では意図メモと整合する選択 (例: m05「ミニマル徹底」「デザイナー向け」) でも、レンダ工程が生成した視覚トークンは黒地×赤 (#0d0d0d/#ff3b1d) 等となり、メモの美的意図 (「白ベースに文字と余白」) と衝突。compiled側はトークンを忠実に実装するため、その衝突が成果物に固定された。rawは指定がない分、無難な出力がメモに近づいたケースが多い。

**これは製品の中核テーゼ「ラベル (言葉) だけでは意図は選べない — だから見本 (帰結) を見て選ぶ」の定量的な裏付けが、測定手続きの欠陥として現れたものである。** v1の手続きは製品の最重要機能 (見本を見てから決める) を省略しており、製品の実UI体験の測定になっていない。

## 追補2 (設計修正案・実行前に記録): v2 = 見本つき選択

- 変更点: 選択セッションで各判断点の**全選択肢の視覚トークン (見本相当)** をレンダして発注者役に提示し、ラベル+見本の両方を根拠に選ばせる (実アプリのUI体験と同等)。他の手続き・判定方法は不変。
- 予測の事前記録: 製品テーゼが正しければ、見本つき選択では美的衝突が回避され compiled の発注者判定勝率が改善するはず。改善しなければそれも公開する。
- v1の結果は破棄せず、「ラベルのみ選択」条件の対照データとして保存・公開する (期せずして良い対照実験になっている)。

## 結果2 (2026-08-01・v2 = 見本つき選択の実測 — 全公開)

- 発注者判定 (主指標): **compiled 4/10** (v1: 2/10 — 見本提示で改善したが逆転には至らず)
- 第三者判定1: compiled 4/10 (v1: 8/10) / 第三者判定2: compiled 2/10 (v1: 5/10)
- 生データ: judgments/*_v2.json・decoded_tally_v2.json。v2の選択はv1から複数の判断点で変化 (見本が判断を変えた実例を choice_record 比で確認可能)。

### 総合解釈 (事前登録した予測との突合)

予測「見本つき選択で compiled の発注者判定勝率が改善する」は**方向としては成立 (2→4) したが、仮説の主命題 (Bの方が意図に近い) は本測定条件では実証されなかった**。判定理由の全文から特定できた構造的原因:

1. **決定の過剰な文面化**: compiled側の制作者が、決定ラベルを成果物の文面にそのまま露出させる (例:「実務2〜4年目って」をメール本文に書く、「かっこよさ/信頼感」を見出しに直訳)。判定者はこれを不自然と減点。→ 下流制作者プロンプト (COMPILED_EXTRA) の「決定はすべて実装せよ」が「決定を語れ」と誤読される課題。
2. **見本トークンの美的幅**: レンダが返すトークンが濃色・強コントラストに偏り、静かな美的意図の受け皿が乏しい (m05ほか)。
3. **rawの無難さが二択で有利**: rawはプレースホルダで非コミットに留まり「穴埋めだけで出せる」と評価されがち。コミットすること自体が製品の目的である以上、強制二択の判定設計はコミットの外れだけを罰し、当たりを十分に報酬しない。

### 限定と扱い

- 本測定は AIペルソナ発注者・sonnet/effort low・n=10 の条件に限る。人間発注者・実運用モデルでの追試は未実施。
- 上記1・2は Wave 2 の具体的な製品改善課題として採用する (①制作者プロンプトに「決定は実装せよ、ただし決定ラベルを成果物の文面に露出させない」を追加 ②レンダの美的幅の拡張)。改善後の再測定 (v3) で本手続きを再利用する。
- 効果の主張はこの結果が覆るまで行わない。提出物・対外文書に「品質が上がる」という言明は載せない (現状の対外主張は「意図の透明化・タイプ0字・検証可能性」であり、これらは本結果と矛盾しない)。

## 追補3 (2026-08-01・実行前に記録): v3 = 原因①の修正のみを検証

- 変更点: 下流制作者プロンプト (COMPILED_EXTRA_PAGE/TEXT) に「決定は実装せよ、ただし語るな (判断点名・選択ラベル・メタな言い回しを可視文面に書かない。ラベルはコメントのみ)」を追加。**選択 (handoff_v2) と raw は固定し、単変数統制とする。**
- 原因② (レンダの美的幅) は選択自体が変わるため v3 では扱わず、Wave 2 の製品開発項目とする。
- 予測の事前記録: 原因①が主要因なら発注者判定の compiled 勝率は v2 (4/10) から改善する。判定は新規エージェント3体・blind_v3 (左右シード変更) で行う。

## 結果3 (2026-08-01・v3 = 原因①修正の実測 — 全公開)

- 発注者判定 (主指標): **compiled 3/10** (v1: 2 → v2: 4 → v3: 3)。事前記録の予測「原因①が主要因なら改善する」は**外れた**。
- 第三者1: 5/10 / 第三者2: 2/10。生データ: judgments/*_v3.json・decoded_tally_v3.json。
- 修正①の効き: メタ文面の粗検出は0件になったが、判定文からm03で「実務2〜4年目のエンジニアへ」のセグメントタグ露出が残存 — プロンプト指示の強制力は部分的。

## 総括 (v1〜v3・シリーズ確定)

1. **本枠組み (AIペルソナ発注者・sonnet/effort low・n=10・強制二択) では、指示書による意図一致の改善は実証されなかった** (主指標 2→4→3/10、揺れはノイズ域)。効果の対外主張は行わない。
2. シリーズを通じて安定して観測された構造 (判定全文より):
   - **原因③ (新規・最重要): 判断点のカバレッジ限界。** 文に係留された3〜5の判断点は、意図メモが含む「文の外の意図」(秋らしさ・内輪の空気・産地の語り口等) を覆えない。compiledは判断点についてはコミットするが、文外意図の外しが二択では致命傷になる。rawの非コミットな無難さが相対的に有利。
   - raw側は事実の捏造 (存在しない日付・遅延理由・社員談話) が繰り返し観測され、compiled側はプレースホルダ規律が効いている — 二択の「意図に近いか」はこれをほとんど報酬しない (指標設計の限界)。
3. **実利用との乖離 (本測定の外的妥当性の限界)**: 実利用では発注者自身が文外意図を知った上で見本を選び、気に入らなければ選び直せる。AIペルソナは固定メモの範囲でしか選べない。人間発注者での追試なしに本結果を実利用へ一般化はできない (逆方向にも)。
4. Wave 2 への設計課題として採用: ①決定の非露出の強制力強化 (実装済み・要改良) ②レンダの美的幅 ③**文外意図の取り込み** — 「タイプ0字」の憲法と矛盾しない形 (例: 見本の選び直し・弾くの解禁、参照トーンの1クリック指定) を検討する。測定手続き自体 (事前登録・ブラインド・全公開) は再利用可能な資産として確立した。

---

## 追補4 (2026-08-10・実行前に記録): v4 = 文外判断点の実装後・実務指標2つでの再測定

### 変更点1: 測定対象 (製品側の変数)

原因③ (判断点カバレッジ限界 = 文外意図を問えない) への製品改修として、抽出エンジンに**文外判断点** (`kind: "beyond_text"`・最大2件・由来1文必須・原文係留なし) を実装した (prompts/extraction_product_v2.txt・コミット済み)。v4 の compiled 条件はこの新エンジンで抽出→見本つき選択 (v2手続き)→`unprompt.compiled_brief.v1` の手渡しJSONを用いる。下流制作者プロンプトは v3 と同一 (COMPILED_EXTRA の非露出規則入り)。raw 条件は選択に依存しないため v1 の成果物を固定再利用する (m11/m12 のみ新規生成)。

### 変更点2: 指標の入れ替え (強制二択を廃止)

v1〜v3 の主指標「どちらが意図に近いか (強制二択)」は、(a) raw の非コミットな無難さを報酬し (b) raw の捏造をほぼ罰しない構造的限界が確定した (総括2)。v4 は決勝ピッチで公約した実務指標2つに入れ替える:

1. **やり直し回数 (主指標)**: 判定者が各成果物を**単品で** (ペア比較でなく・条件ラベル非表示・決定論順) 受け取り、発注者メモを根拠に「発注者として差し戻したい点」を列挙する (0件可・各項目に対象箇所と理由を必須)。集計はブリーフごとの件数比較 (少ない方が勝ち・同数は引き分け) と全体平均。
2. **捏造率 (副指標)**: 各成果物について「ブリーフからも手渡しJSONからも導出できない具体的事実 (日付・時刻・固有名・数値・実績・談話引用)」を列挙する。導出可否の基準は**両条件とも同一** (brief + handoff の和集合。raw 制作者は handoff を見ていないが、判定規則を条件間で変えないことを優先する)。プレースホルダ (〈〉) と一般常識は捏造でない。集計は捏造件数/成果物。判定者には条件ラベルを渡さない。

### 変更点3: ブリーフ12本 (事前登録候補プール内で+2)

m01〜m10 (継続) に、事前登録済み候補の残り **m11-brand・m12-faq** を追加採用する。理由: 文外意図 (世界観の空気・安心感の設計) が効きやすい成果物型であり、追補1で機械的に不使用とした2本をプール内から起用することで恣意選定を避ける。m11/m12 の発注者メモは explode 実行前に独立エージェントが依頼文のみから生成し owner_memos.md へ追記する (既存10本と同一規律)。

### 手続き (変更のない部分は v2/v3 を継承)

1. owner_memos.md へ m11/m12 を追記 (選択肢を見る前に固定) → 本追補とともにコミット。
2. 新エンジンで explode (12本・`_v4`)。文外判断点の出現数・棄却は explode JSON にそのまま残す。
3. 見本つき選択 (v2手続き): 全選択肢の視覚トークンをレンダ提示→発注者役エージェントがメモのみを根拠に決定。文外判断点も同一UX (決める/委ねる→本版は全て決める)。
4. compile→handoff_v4 (`unprompt.compiled_brief.v1`・kind/origin_rationale/generator_models 入り)。
5. 生成: compiled_v4 = 12本新規 / raw = v1 固定再利用 (m11/m12 のみ新規)。モデル・effort・骨格は従来同一 (sonnet・low)。
6. 判定: 独立エージェント3体。指標1はメモあり・指標2はメモなし (事実性はメモと無関係)。成果物は単品提示・条件ラベルなし・提示順は id+シードの決定論シャッフル。
7. 生データ全公開 (負けても): sessions/*_v4・pairs_v4/・judgments/*_v4.json・tally_v4.json。

### 予測の事前記録

1. **主指標**: 文外判断点が原因③の差し戻し (「秋らしさがない」「内輪の空気がない」型) を減らし、やり直し件数で compiled ≦ raw のブリーフが過半になる。
2. **副指標**: 捏造件数は raw > compiled (v1〜v3 で反復観測された raw の捏造が、この指標で初めて数値として現れる)。
3. 外れた場合もそのまま公開し、原因分析を追記する。

### 開示・限定

- 発注者・判定者は AI ペルソナであり人間の実測ではない。n=12・sonnet/effort low・1発注者相当。統計的一般化はしない。
- 複数モデル判定 (エンジン中立性の実証) は AI&クレジットの承認後に追加走として実施予定であり、本走は sonnet のみ。
- v1〜v3 の成果物・判定は破棄せず対照として保存する。

## 結果4 (2026-08-10・v4 = 文外判断点+実務指標の実測 — 事前登録どおり全公開)

- 抽出実測: 12本中11本で文外判断点が1件ずつ出現 (m08-apology は 0件 — 上限まで埋めない挙動も観測)。棄却0件。
- **指標1 やり直し件数** (判定3体・平均/成果物): compiled **3.08** / raw **2.89**。ブリーフ別勝敗 compiled 4 / raw 6 / 引き分け 2。**予測1 (compiled≦raw が過半) は外れ** (compiled勝ち+引き分け 6/12 で過半に届かず)。
- **指標2 捏造件数**: compiled **3** / raw **1**。**予測2 (raw > compiled) は外れ (逆)**。
- 生データ: sessions/*_v4・pairs_v4/・blind_v4/・judgments/judge{1..3}_v4.json・tally_v4.json・picks_v4.json。
- 手続き開示: 判定呼び出し失敗4件 (rework) は同一手順で再実行して埋めた (結果を見ての再判定ではない)。m05 compiled にモデル起源の実メールが再出現し判定後に伏字化 (SANITIZATION.md 追記・判定文への波及なしを grep で確認)。

### 原因分析 (判定全文に基づく)

1. **文外判断点は狙った症状 (原因③) には効いた形跡がある。** v1〜v3 で繰り返し差し戻された文外意図の外しの代表例で compiled が明確に勝った: m02-event (秋・内輪の空気) **0.0 vs 2.7**、m06-news (季節の雑談) 1.3 vs 3.0、m11-brand 4.3 vs 4.7。m02 は3判定とも compiled 差し戻し0件。
2. **新たな負け筋(a): プレースホルダ規律の意図衝突。** 最悪ケース m12-faq (compiled 5.0 vs 2.7) は「〈返品ポリシーページ〉参照」「〈返品受付期限〉」等、条件の実質を書かない誠実プレースホルダが、メモの意図「安心感=条件の明示」と正面衝突した。raw は具体を書いてしまうぶん意図に近く見える — それは v1〜v3 で観測した捏造の温床と同じ現象の裏表であり、2指標が同一トレードオフの両面を測っていることが確認された。
3. **新たな負け筋(b): 意図の充足が創作を誘発する。** compiled の捏造3件 (m06「給湯室の冷やし飴」×判定2体・m07「最初の3か月」) は、意図 (季節の雑談から入る・失敗談を混ぜる) を実装しようとして固有エピソードを創作したもの。文外判断点で意図が濃くなるほど下流は「事実」で埋めたくなる。
4. **指標2の較正不足。** raw の捏造が1件 (m06 の「8月」) しか出なかったのは v1〜v3 の反復観測 (日付・遅延理由・談話) と不整合であり、判定規則の除外項 (プレースホルダ・一般構造・デザイン創作) と判定者の保守的運用が原因の可能性が高い。raw 成果物は v1 と同一物である。この数値から「raw は捏造しない」とは言えず、言わない。

### v5 候補 (実施は別途・事前登録追補を先に切る)

- COMPILED_EXTRA に二律を追加: 「固有エピソード・具体的事実は創作せずプレースホルダにする」+「ただし条件・手順の骨子は参照へ丸投げせず実質を書く」(負け筋(a)(b)の同時対処)。
- 捏造判定の較正: 既知の捏造を仕込んだ陽性対照成果物で判定器を先に検証してから本走する。

## 追補5 (2026-08-19・実行前に記録): v4追加走 = 複数モデル判定 (判定器のエンジン中立性)

追補4の開示・限定に記した「複数モデル判定は AI&クレジットの承認後に追加走として実施」の実施登録。**成果物・入力・判定文は v4 と同一 (バイト同一) で、判定器のモデルだけを Claude 系以外に差し替える**。v4 本走の「Claude 製の指示書を Claude が判定する」という自己言及の懸念に対し、判定の方向が判定器のエンジンに依存しないかを測る。

### 判定モデル (3・開発元が相異なり Claude 系を含まない)

ai& API (api.aiand.com・OpenAI互換) 経由。`reasoning_effort` は全呼び出し `"high"` を明示 (モデルごとに既定が異なり、放置すると条件が揃わないため)。その他のパラメータは API 既定。

1. `deepseek-ai/deepseek-v4-flash`
2. `zai-org/glm-5.2`
3. `moonshotai/kimi-k3` (計画時点の候補 kimi-k2.6 は 2026-08-19 実測で提供終了のため、同系後継の k3 を採用。コード特化の k2.7-code は判定用途に不適合として不採用)

### 手続き

1. 各モデルで判定3体×2指標×24成果物 = **144呼び出し** (3モデル計432)。判定3体は API 既定 temperature の独立呼び出しで、システム文中の判定者番号 {jid} のみ相違 (v4 の「独立エージェント3体」に対応)。
2. 実行道具は同梱の `judge_ext.py`。システム文 (REWORK_SYS / FABRICATION_SYS)・ユーザー文の組み立ては `judge_v4.py` と同一文字列。入力は blind_v4/ (単品・条件ラベルなし)・owner_memos.md・sessions/*/handoff_v4.json をそのまま使う。
3. 先頭10呼び出しは本番入力そのままの較正走 (トークン量と実費/呼び出しを実測してから全量を流す)。較正走の判定結果もそのまま本データに算入する (入力・手順が同一のため)。
4. 失敗 (API エラー・JSON 不読・出力打ち切り) は v4 と同じく同一手順での再実行で埋め、件数を開示する。結果を見ての再判定はしない。
5. 生データ全公開: judgments/ext/ (判定全文・モデル別) + judgments/ext/costs_v4ext.jsonl (全呼び出しの実費記録)。

### 集計と一致の物差し (実行前に固定)

モデルごとに v4 と同じ集計 (指標1: 平均やり直し件数と ブリーフ別勝敗 / 指標2: 捏造件数) を出した上で:

1. **方向一致 (主)**: 全体平均の大小関係 (compiled vs raw) が sonnet 本走と一致するか。指標ごと・モデルごとに判定。
2. **ブリーフ別勝敗の一致率**: 12ブリーフの勝/負/分が sonnet 本走と一致した割合。
3. **捏造検出の重なり**: sonnet 本走の捏造4件 (compiled 3 / raw 1) を各モデルも検出したか (対象箇所ベース)。

### 予測の事前記録

1. 指標1の方向 (raw < compiled = raw が良い) は 3モデル中 2 以上で一致する (v4 の差 0.19 は小さく、全モデル一致までは予測しない)。
2. 指標2の捏造件数の絶対値はモデル間で大きく揺れる (追補4 原因分析4の較正不足が判定器側の性質であることの傍証になる)。件数の絶対値は解釈せず、重なりのみ見る。
3. 外れた場合もそのまま公開し原因分析を追記する。

### コスト開示 (クレジット使途の記録)

- 全呼び出しに `X-Aiand-Metrics: true` を付与し、応答の `x-cost` (円) を costs_v4ext.jsonl へ記録。総和と、実行前後のコンソール残高差分の二重測定で実費を確定する。
- 価格実測 (2026-08-19・円/100万トークン・入力/出力): flash 25/40・glm-5.2 160/650・kimi-k3 480/2000。事前見積は総額 2,000〜3,000円 (上限 16,000円で承認済み)。

### 限定

- 本追補は**判定器の検証**であり、成果物・製品側は一切変更しない。v4 の限定 (n=12・AIペルソナ・統計的一般化なし) を継承する。
- 3モデルは ai& がホストする open-weight 版であり、各開発元の公式 API とは量子化・サービング差がありうる。
