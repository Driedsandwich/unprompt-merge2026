# COCREATION_PAGE — ブリーフ・ゼロ文書(デック⑦の原稿)

下書き: 2026-07-29(Day1夜)。1枚図として Day3 にスライド化。

**主張(1行)**: この製品の開発自体が、製品と同一の思考様式 — **文 → 分岐 → 決定** — で進んだ。以下の引用はすべて実在のログ(PROCESS_LOG.md の3列表と、判断点タグ付き git コミット)からの転記であり、捏造はない。

## ブリーフ・ゼロ(開発の最初の曖昧な一文)

> 「3日で、AIが成果物を書かない意図コンパイラを作る」

この一文が、開発初日にどの判断点(分岐)へ分解され、どう決定されたか。製品が曖昧語をカードに爆散させるのと同じ文法で示す。

## 文 → 分岐 → 決定(実ログからの引用で構成)

各分岐の出典: git コミットの `resolved:` 行(判断点タグ)を原文のまま引用。

### 分岐1〈3日で〉→ 実行アーキテクチャ

> `resolved: 実行アーキテクチャ→ビルドレス(npm依存ゼロ) / 理由: 3日実装の確実性`(コミット 68b3d01)

### 分岐2〈誰でも動く〉→ 実行エンジン

> `resolved: 実行エンジン→Claude Code サブスクリプション headless CLI(claude -p) / 理由: APIキー不要・審査員再現性`(コミット 68b3d01)

背景(PROCESS_LOG より): 「ユーザー制約(Anthropic APIキーを用意できない)による」→ 採用。ブラウザ直叩きはAPIキー保有者向け代替経路へ降格し、DISCLOSURE.md に開示。

### 分岐3〈意図コンパイラ〉→ 抽出スキーマの形状(レイテンシとの戦い)

品質は初回から合格線超過(anchor違反0・捏造0)だったが、レイテンシが設計想定の10倍。**実測に基づく4段階の決定**で解決した:

| 段階 | 構成(決定) | 実測 |
|---|---|---|
| 0 | 初期(--append-system-prompt・effort既定・リッチスキーマ) | api 97〜123秒 / 出力8〜10K tok |
| 1 | --system-prompt 置換 + --effort low | api 44.5秒 / 2.7K tok |
| 2 | スリムスキーマ分離(extraction_product_v1.txt 新設) | api 8.8〜15秒 / 530〜540 tok |
| 3 | --setting-sources project(起動オーバーヘッド遮断) | 壁時計 ≈ api + 約1秒(従来 +13〜27秒) |

> `resolved: 抽出レイテンシ問題→スキーマ分離(検証用フル/製品用スリム)+--system-prompt置換+--effort low / 理由: 出力トークン量がボトルネックと実測で確定(8-10K→2.7K tok)`(コミット 741f21a)

> `resolved: Gate1切替判定→主経路続行 / 理由: 品質合格線を大幅超過(22実行でanchor違反0・捏造0)、レイテンシは工学解決(110s→9-16s、起動OH 1s化)`(コミット d9d3c0b)

### 分岐4〈成果物を書かない〉が要求する対照レンダ → レンダモデル

> `resolved: レンダモデル→sonnet固定 / 理由: haikuはeffort lowでも多弁(40秒/3.5Ktok)、sonnetは4.1秒で簡潔・対照的`(コミット 006ce1d)

修正後実測(PROCESS_LOG より): レンダ api 4.1秒 / 壁5.0秒(10分の1)。

### 委任の例(製品の〈委ねる〉に相当)

AI(実装セッション)の提案を人間(管制塔)が承認し、判断根拠ごとログに残した例(PROCESS_LOG 3列表・原文):

> 「キラーテスト(抽出品質の実測)を、スクリプト実行ではなく人間がブラウザで操作するハーネス(単一HTML)として実装する」— 管制塔が承認 — 「採用。有効分岐の意味判定は人間(管制塔)が行う設計のため、機械判定と人間判定を同一画面で完結させるのが最短」

## スライド化の視覚案

- 中央に開発の一文(ブリーフ・ゼロ)。製品UIと同じく、文中の語〈3日で〉〈誰でも動く〉〈意図コンパイラ〉〈成果物を書かない〉から SVG テザーで4枚の分岐カードを係留。
- 各カードに「決定(resolved: 行の引用)+理由+実測値」。決定済みの語は製品と同じ確定色で塗る。
- 右下に小さく: 「このスライドの引用元: PROCESS_LOG.md / git log(リポジトリで検証可能)」。
- レイテンシ4段階表は分岐3のカード内にミニ折れ線(97-123s → 44.5s → 8.8-15s → +1s OH)として描く。

## 誠実性の注記

- 引用はすべて原文転記。「ブリーフ・ゼロの一文」自体は本ページのための要約表現であり、ログ原文ではない(スライドでは「開発ミッションの要約」と脚注を付す)。
- 意味判定(分岐の有効性・品質の合否)は常に人間=管制塔が行い、AIの提案・実測・人の裁定は PROCESS_LOG の3列(AIの提案/人の判断/採否と理由)で分離記録されている。
- 生データ: EVIDENCE/killer_test/(結果JSON・SUMMARY・GATE1_DECISION.md、未編集)。
