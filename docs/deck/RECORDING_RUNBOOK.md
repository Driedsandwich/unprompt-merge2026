# RECORDING_RUNBOOK — 収録手順書(2026-07-31)

対象: VIDEO_SCRIPT_DRAFT.md の90秒絵コンテを、等倍・ノーカット・実測で収録する。

## 0. 環境準備(収録直前に毎回)

1. サーバ再起動: `lsof -ti :8512 | xargs kill; nohup python3 server.py --port 8512 >/tmp/rec8512.log 2>&1 &`(常駐で起動OHを amortize、開示#6)
2. ブラウザ: Chrome の**新規ウィンドウ**、ブックマークバー非表示(⌘⇧B)、ウィンドウ 1280×800 目安。タブは http://127.0.0.1:8512/ のみ
3. テーマ: **明**(製図白が本製品の顔。右上トグルで固定)
4. ウォームアップ: 収録に使わない例文で1回爆散→ホーム帰還(モデル側のコールドスタート回避+claude-sonnet-5 を内訳の固定情報に観測させる)
5. 画面収録: QuickTime(⌘⇧5)→ウィンドウ指定、マイクなし(無音収録。音は付けない方針でも可)
6. 通知を切る: macOS 集中モード

## 1. 収録パス(ワンテイクの操作列)

| CUT | 操作 | 待ち/確認 |
|---|---|---|
| 1 | 入力欄クリック→「モダンだけど温かみのあるLPを作って。うちの会社のやつ。」を**手でタイプ**→問い返させる | タイプは焦らず自然速度(この一文が最初で最後のタイプ) |
| 2 | 何もしない | 静寂カウンタが実測を刻む。S=初回カード出現秒(編集で控える) |
| 3 | 何もしない | 4分岐が着地、圏点・引出線 |
| 4A | 「温かみ」カードの見本を1枚クリック | 語が藍に、カウント−1 |
| 4B | 別カードの「委ねる」をクリック | 語が鼠に、カウント−1 |
| 5 | 「AIのおすすめで決める」をクリック | ボタンが秒を刻む→残りが藍に→理由の一行→未決0 |
| 6 | 「指示書にまとめる」→読む用を2〜3秒→軽くスクロール→CTA(完成品ではありません+コピーしてAIに渡す)を映す | ヘッダ統計「タイプ0/選択N」を確実にフレームに |
| 7 | 「この指示書で作らせた実例を見る」 | lp-warm ペア表示。右側の「温かみ」反映箇所に**カーソルで円を描いて**注視誘導(ズームは編集で) |
| 8 | (別撮り可)ターミナルで `git log --oneline -8` と PROCESS_LOG.md の3列表を2〜4秒 | メタショット |
| 9 | (編集で)ロックアップ画面 | 静止でも可: ホームに戻って Unprompt ロックアップを2秒 |

失敗時: 最初からやり直してよい(ロゴ→やり直す)。**テイク間でサーバは殺さない**。使用テイクの決定は「S が短い」ではなく「操作が自然」を優先(S は実測のまま採用し、編集で絵コンテの時刻を再割付)。

## 2. 収録後すぐ

- [ ] 使用テイクの S(送信→初回カード秒)と全確定秒を控える → VIDEO_SCRIPT の時刻表を実測で更新
- [ ] レイテンシログ: `logs/session_*.jsonl` の該当セッションをそのままコミット(動画タイムスタンプと突合可能に)
- [ ] スクショ3点(ホーム/爆散/並置)を README 用に書き出し

## 3. 編集(テロップ焼き込み)

- 全カットに VIDEO_SCRIPT のテロップを焼く(ミュート一次審査対応)
- CUT 2 に極小バッジ「x1.0 — 生成は、始まらない」+経過秒(画面内カウンタが映っていればそれで足りる)
- 語彙統制: 「確認質問/明確化/プロンプト改善」を全テロップで不使用(必須変更8)
- 書き出し: 1080p / H.264 / 90秒以内

## 4. Gate 3 検証(提出前必須)

- [ ] ミュートで通し視聴(意味が全部伝わるか)
- [ ] 2倍速で通し視聴(静寂が事故に見えないか=バッジ確認)
- [ ] タグラインがスコープ限定形か(「あなたの成果物を」付き)
- [ ] 事前生成の注記が並置カットで読めるか
