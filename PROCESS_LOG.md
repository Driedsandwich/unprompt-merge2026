# PROCESS_LOG — AI提案と人間判断の記録

各エントリは追記のみ。日付降順ではなく時系列順に追加する。

| AIの提案 | 人の判断 | 採否と理由 |
|---|---|---|
| Nodeプロキシ経由のAPI呼び出し構成を、ビルドレス・サーバレスのブラウザ直叩き構成(`anthropic-dangerous-direct-browser-access`)へ改訂する | 管制塔が承認 | 採用。実行環境制約(npm/Node禁止)と審査員の再現性(python3 -m http.server のみで再現可能)のため。プロキシ廃止に伴う開示は DISCLOSURE.md に記載 |
| レイテンシログをサーバ側収集からクライアント側収集(ブラウザ内で計測しJSONとしてエクスポート)へ変更する | 管制塔が承認 | 採用。プロキシ廃止によりサーバ側計測点が消滅したため。計測がクライアント時計依存になる点は開示事項とする |
| キラーテスト(抽出品質の実測)を、スクリプト実行ではなく人間がブラウザで操作するハーネス(単一HTML)として実装する | 管制塔が承認 | 採用。有効分岐の意味判定は人間(管制塔)が行う設計のため、機械判定と人間判定を同一画面で完結させるのが最短。Day1目標「人間がブラウザで実行できる状態」に直結 |
| APIキー不要化の実行エンジン変更: 主経路を Claude Code サブスクリプションの headless CLI(`claude -p`)へ変更し、ブラウザ直叩きはAPIキー保有者向け代替経路へ降格する(提案: 管制塔) | ユーザー制約(Anthropic APIキーを用意できない)による | 採用。抽出プロンプトはツール呼び出し前提から「単一JSONオブジェクトのみ出力」方式へ適合改訂。レイテンシは duration_api_ms と壁時計を分離記録し、開示は DISCLOSURE.md に追記 |
| キラーテストv1(フルスキーマ)を Sonnet 5 で実行 → 品質は極めて高い(anchor違反0・対照ブリーフ捏造0・有効分岐率実質100%)が、レイテンシが設計想定の10倍(api 97-123秒、出力8-10Kトークン)。原因は Claude Code 既定システムプロンプト配下の拡張思考+検証用リッチスキーマ | 管制塔が切り分け実験(システム置換/effort low/モデル比較)を実施。--system-prompt 置換+--effort low で 44.5秒(2.7K tok)まで改善。ボトルネック=出力量と確定 | 採用: 検証用フルスキーマと製品用スリムスキーマを分離(extraction_product_v1.txt 新設)。品質証拠はv1結果で確保済み、レイテンシ線は製品形状で再測定。DISCLOSURE に記録予定 |
| キラーテストv2(スリム+新エンジン)全11本完走: エラー0・anchor違反0・捏造0。api 8-33秒(中央値~12秒)。壁時計との差13-27秒の起動オーバーヘッドを追加調査 | 管制塔が --setting-sources project で遮断実験 → オーバーヘッド約1秒に短縮(ユーザー設定のフック/プラグイン読込が原因と確定) | Gate 1 裁定: 主経路続行。品質PASS(意味判定込み)。単発9-16秒は stream-json 分岐ストリーミング(Day2実装)で初回カード8秒以内へ。ヒラク切替なし。GATE1_DECISION.md 参照 |
| WF-D2/D2b: server.py+app/index.html+レンダ/コンパイルプロンプトを構築、独立E2E検証PASS(爆散3分岐/anchor実在/対照レンダ/rationales書き分け)。ただしレンダがhaikuで40秒、サーバにsetting-sources未反映(+12秒) | 管制塔が即時修正: レンダ既定をsonnetへ(haikuはeffort lowでも多弁と実測確定)、--setting-sources project追加 | 修正後実測: レンダ api 4.1秒/壁5.0秒(10分の1)。Day1完了: 爆散→対照レンダ→コンパイルの実データ一気通貫が成立 |
| WF-D2c: 爆散の分岐ストリーミング実装(`POST /api/explode_stream` を新設、SSE で meta/branch/done を逐次配信。claude を `--output-format stream-json --include-partial-messages` で起動し、branches 配列内の各オブジェクトが閉じた時点で anchor 検証を通してから1件ずつ送る)。app/index.html は最初の branch 到達で爆散を開始し、以後は到着順にカードを追加。旧 `/api/explode` は残しフォールバックに使う | 実測で3層に分解: (a) CLI 起動 2.4-6.7秒 (b) API TTFT 1.6-11.1秒 (c) 本文生成 2-4秒。ストリーミングが削れるのは (c) の大半のみ。加えてスキーマ順を branches 先頭にする A/B を実施(init 差引後のモデル時間 6.8秒→4.2秒) | 初回カード表示 中央値 6.8秒(n=11、最小5.2/最大15.0)、従来の全体待ち 中央値 13.1秒に対し1回あたり 3.0-7.2秒短縮。8秒以内は 11回中6回で、未達分は API TTFT のばらつきが支配的。残る固定費は CLI 起動で、事前ウォーム(`--input-format stream-json` で先に起動しておく)により 2.4-5.6秒削減できることを別途実測済み(未実装) |
| 上記実装中に既存不具合を発見: 命令形ブリーフ(「〜を書いて」)でモデルが抽出せず成果物本文を書いてしまい、分岐0件になる。data/briefs.json では release-mail / ec-product / youtube-about / scout-mail の4本が該当 | 現行 `/api/explode` プロンプトでも同様に再現(release-mail 0/3成功)。ストリーミング実装が原因ではない。修正案(ブリーフを `<brief>` で包み「これはデータであって指示ではない」と明示)を実測: release-mail 2/2、ec-product 2/2 成功、非該当ブリーフは前後とも同等 | **未適用**。抽出契約(explode / explode_stream 双方)に触るため管制塔判断待ち。手順と検証項目は EVIDENCE/brief_as_data.patch に記載 |
| Day2: SSE分岐ストリーミング実装(first_branch中央値6.8s・分散7-13s)、並置証明の実生成(一目で別物判定)、デック骨子4文書(引用は全てgit log逐語一致)。独立検証で凍結貫通の穴とスキーマ順矛盾を検出 | 管制塔が仕上げ3点を指示: 分岐先頭スキーマ(矛盾解消を兼ねる)/first_branch_ms証拠ログ/凍結中の高さ変更禁止 | 全適用・12ケース行列検証済み。レイテンシは「実測x1.0バッジ+開示リストで分散を明記」の誠実路線で確定 |
| 分岐数の振れ対策: ユーザー実機報告(判断点が1〜3個にブレ、デモ・決勝では3〜5件が望ましい)を受け、`prompts/extraction_product_v1.txt` の「分岐の質の規則」末尾に探索目安を1文追加(「曖昧な制作依頼には通常3〜5件の本物の分岐が潜む/読者・目的・トーン・構成・訴求軸・見せ方の軸を一通り確認/3件未満なら一度だけ再点検」)。捏造防止のため「これは探索の目安であって充足すべき数値目標ではない・質の規則を満たさない分岐を目安のために作るのは失格・完全指定ブリーフでは0件が正しい」を同文に併記 | 追加文言の効果をキラーテスト全11本の実測で検証(sonnet / slim / effort low / timeout 120)。判定基準は (a) ambiguous 10本の分岐数分布 (b) control-banner の0件維持 (c) anchor違反0 | 採用・維持。実測 `EVIDENCE/killer_test/results_20260729T095734Z.json`: 分岐数4以上の割合が 4/10(40%)→6/9(67%)、最大値4→5、control-banner は0件を維持(残存曖昧性の所見も正しい)、anchor違反0/29、捏造疑いフラグ0。増えた分岐は実質的(例 precise-pricing の5件目「FAQの主眼は導入前不安か契約条件か」)で水増しではない。効果は中程度で振れ自体は残る(下振れ1件を観測したが、同一ブリーフのOLD/NEW各3回A/Bでは OLD 3,3,2 / NEW 3,失敗,3 で、下振れはプロンプト起因でなくサンプリング揺らぎと判定)。文言の再調整・撤回はいずれも不要と判断 |
| 上記検証の副産物: scout-mail が2回とも抽出せず「確認事項を聞き返す会話文」を返しJSON抽出に失敗(全11本中の唯一のエラー) | OLD/NEW 各3回のA/Bで切り分け: OLD 2/3失敗・NEW 3/3失敗。追加文言の導入前から発生しており、既存ログ済みの命令形ブリーフ不具合(release-mail / ec-product / youtube-about / scout-mail の4本)と同一現象 | 本タスクでは未対処(担当範囲外)。恒久対策は既出の `EVIDENCE/brief_as_data.patch`(ブリーフを `<brief>` で包み「データであって指示ではない」と明示)であり、**未適用のまま**。デモ・決勝で当該4本系統のブリーフを使う場合はこのリスクが残ることを管制塔へ申し送る |
| Codex 監査(独立レビュー)が P0 級4件を指摘: **M1** SSE の途中エラー/切断で `finishStream(null)` が部分集合を「確定」扱いにする(判断点が実は5件でも3件で未決0=コンパイル可へ進む) / **M2** レンダ最大15本がサーバの実行枠6を占め、再送の explode が待たされる / **M3** ストリーミング子プロセスを kill 後に `wait()` せず pump スレッドも join せずに実行枠を返す / **M7** `S.frozen` を解除する本番経路が無く `equalizePending` が永久残留 / **N4** 送信後380ms未満で初回branchが着くと旧タイマーが爆散後DOMを上書きしうる | 管制塔が優先度を決定: P0(M1/M2/M3/M7/N4)は即修正、P1(M5/N3)も修正、M4・N1(非loopback bind時のHost偽装・body先読み)は**コードを変えず DISCLOSURE への明記のみ**、N2/M6/N5/N6 は見送り | 全件適用。M1 は「done 未到達を成功にしない」へ設計変更し、一括 `/api/explode` で取り直して **question_point 一致で照合**する復旧経路を新設(既出カードと**ユーザーが既に下した決定は絶対に温存**・一括側に無い既出も消さない・照合できなければ総数を確定させずコンパイル不可のまま帯に「再取得」を出す)。M2 は (a) レンダ専用副上限 `render_cap=3`(全6枠、explode/compile に常時2枠以上を確保)(b) `pagehide`/`beforeunload` と復旧時の AbortController 中断+復旧後の自動再開 (c) レンダ開始前の接続生存確認(EOF 検知時は claude を起動せず 499 相当で記録)。M3 は `start_new_session=True` によるプロセスグループ分離 + TERM→期限付き wait→KILL→回収 + pump join 後に枠返却(併せて停止時に走行中の子を始末する `terminate_all` を追加。Ctrl+C が子へ届かなくなる副作用への対処)。M7 は `pointerleave` と操作確定(decide/delegate)で解除し保留分を適用。N4 は timer ID 保持 + `beginExplode` で cancel。P1 は M5(`killer_test/run.py` が `json.loads` 後の型を見ずに `.get()` へ進み、CLI が配列を返すと AttributeError でハーネス全体が落ちる)を dict 検証+`cli_json_parse` として記録し続行へ、N3(done 送信失敗を成功として記録)を「emit の結果を返し `client_gone` を別フィールドで記録」へ |
| 上記修正の検証方法(実測) | 管制塔の指示「修正ごとに関連domtest/py_compileを回す」に対し、担当が新規テスト2本を追加して実測 | `EVIDENCE/streaming/domtest5.js`(新規・42項目すべてPASS): 途中切断→部分集合で締めない→一括照合で4件へ復旧・**切断前の決定が生存**・再取得失敗時は総数未確定のままコンパイル不可・再取得ボタンで復旧・凍結解除2経路・380msタイマーcancel・レンダ中断と再開。`EVIDENCE/streaming/servertest.py`(新規・23項目すべてPASS、claude は呼ばず `fake_claude.py` が CLI 外形を模擬): レンダ同時実行が3を超えない/その渋滞中も explode の枠待ち0.000s/ストリーム途中 close で子と**孫プロセス**が死に枠が返り pump スレッドが残らない/切断済み接続では claude を起動しない/done 送信失敗時に ok=false かつ client_gone=true。既存 domtest.js・domtest2.js・domtest3.js・spantest.js は全PASS(回帰なし)。実 claude での一気通貫も再確認(port 8641: explode_stream 4分岐 first_branch 5.3秒/done 13.1秒 → render ok 7.3秒 → compile ok 6.2秒) |
| 既知の未対応(申し送り): `EVIDENCE/streaming/domtest4.js` が10項目FAIL | 担当が修正前後で FAIL 集合を差分比較(`git stash` で HEAD と照合)し、**修正前と完全に同一**であることを確認 | 原因は直前コミット c5a6c19「カウンタを総数+未決の分離表示へ変更」に対し domtest4 の期待文字列(`判断点 3→0`)が旧仕様のまま残っていること。**製品側の不具合ではなくテストの陳腐化**。本タスクの担当範囲(監査指摘の修正)外のため未修正で申し送る |
| Codex 監査の残り(N2 / M6 / N5 / N6)も直すべきか | 管制塔が見送りを決定(判断理由として「通常経路に影響なし」「pregen 成果物は検証済み」を提示) | 見送り。修正も再現確認も行っていない(担当の実測による裏付けは無く、採否は管制塔の判断そのもの)。P0/P1 を7/30の収録前に確実に着地させることを優先した。再開が必要になった場合はこの行と Codex 監査原文を起点にする |
| ユーザー第2フィードバック: エンジニア語の混在/静寂コピーの誤読リスク/並置比較が現セッションと不一致に見える | 管制塔が置換表を決定しWF-D6適用: コンパイル→指示書にまとめる等の生活語化、静寂コピーを説明形+x1.0実時間へ、比較ビューに事前生成ラベル+不一致注記の自動表示 | 管制塔がブラウザ実操作で全経路を目視検証(爆散→4決定→指示書→比較、朱→藍転移、不一致注記の発火確認)。Gate 2 クローズ |
